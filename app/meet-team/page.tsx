import MeetTeamClient from "../about/team-client";

export const metadata = {
  title: "Meet the Team | LifeLine",
  description:
    "Get to know the passionate people behind LifeLine’s mission to make healthcare accessible for every Nigerian.",
};

export default function MeetTeamPage() {
  return <MeetTeamClient />;
}
